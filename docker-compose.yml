version: "3"

services:

  gateway:
    build: ./gateway
    ports:
      - "5000:5000"
    depends_on:
      - payment

  payment:
    build: ./payment
    ports:
      - "5001:5001"
    depends_on:
      - database

  database:
    build: ./database
    ports:
      - "5002:5002"